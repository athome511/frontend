import React from 'react'

const BaseURL = React.createContext({})

//BaseURLContext.provide　で行けるかの実験
//export const URLProvider = BaseURL.Provider
//export const URLConsumer = BaseURL.Consumer
export default BaseURL
